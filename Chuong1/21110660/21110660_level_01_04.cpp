#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>
#include <string>
#include <math.h>

#define ll long long
#define vint vector<int>
#define vlong vector<long>
using namespace std;

int main()
{
    ll a, b;
    cin >> a >> b;
    if (a > b)
    {
        cout << a << endl;
    }
    else
    {
        cout << b << endl;
    }
    return 0;
}